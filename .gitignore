# 배포 및 빌드 관련 (안에 들어있는 커다란 파일들 제외)
dist/
build/

# 환경 변수 및 비밀번호 (절대 깃허브에 올리면 안 됨)
.env

# 파이썬 가상환경 및 캐시
.venv/
__pycache__/
*.pyc

# 실행 로그 (계속 쌓이는 텍스트 파일)
app.log

# 사용자가 저장한 메일 보고서 (회사 기밀 유지)
Saved_Reports/
